// In the Create event of a controller object or Game Start event
global.resources = [];
global.resources[RESOURCE.WOOD] = 10;

// Define the resource enum at the beginning of your code
enum RESOURCE 
{
    WOOD
}
